[project]
name = "radiobrowser-api-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "grpcio>=1.83.1",
    "mcp>=2.1.1",
    "protobuf>=7.0.0",
]

[dependency-groups]
dev = [
    "grpcio-tools>=1.83.1",
]
